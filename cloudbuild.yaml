options:
  machineType: "E2_HIGHCPU_8"
  logging: CLOUD_LOGGING_ONLY

timeout: 1200s

steps:
  - name: "gcr.io/cloud-builders/docker"
    id: "build"
    args:
      - "build"
      - "-t"
      - "${_REGION}-docker.pkg.dev/$PROJECT_ID/medlab-repo/report-service:$SHORT_SHA"
      - "-t"
      - "${_REGION}-docker.pkg.dev/$PROJECT_ID/medlab-repo/report-service:latest"
      - "."

  - name: "gcr.io/cloud-builders/docker"
    id: "push"
    args:
      - "push"
      - "--all-tags"
      - "${_REGION}-docker.pkg.dev/$PROJECT_ID/medlab-repo/report-service"
    waitFor: ["build"]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    id: "deploy"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "medlab-analyzer-report-service"
      - "--image=${_REGION}-docker.pkg.dev/$PROJECT_ID/medlab-repo/report-service:$SHORT_SHA"
      - "--region=${_REGION}"
      - "--platform=managed"
      - "--allow-unauthenticated"
      - "--set-env-vars=NODE_ENV=${_ENVIRONMENT},PROJECT_ID=$PROJECT_ID,BUCKET_NAME=$PROJECT_ID-reports,LOG_LEVEL=${_LOG_LEVEL}"
    waitFor: ["push"]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    id: "health-check"
    entrypoint: "sh"
    args:
      - "-c"
      - |
        URL=$(gcloud run services describe medlab-analyzer-report-service --region=${_REGION} --format='value(status.url)')
        curl -f $$URL/health || exit 1
        echo "report-service is healthy"
    waitFor: ["deploy"]

substitutions:
  _REGION: "us-central1"
  _ENVIRONMENT: "dev"
  _LOG_LEVEL: "info"

images:
  - "${_REGION}-docker.pkg.dev/$PROJECT_ID/medlab-repo/report-service:$SHORT_SHA"
  - "${_REGION}-docker.pkg.dev/$PROJECT_ID/medlab-repo/report-service:latest"
